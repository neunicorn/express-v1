    <div class="container">
        <h1>Hello World!</h1>
        <p class="container">welcome to EXPRESS WEBSERVER.</p>
        <p>coba aer</p>
        <h2>Halo <%= nama %></h2>
    
        <h4>Daftar Mahasiswa</h4>
        <% if (mahasiswa.length < 1) { %>
         <p>Data Mahasiswa kosong</p>
        <%} else { %>
            <% mahasiswa.forEach(mhs => { %>
                <ul>
                   <li>Nama: <%= mhs.nama %> </li>
                   <li>Email: <%= mhs.email %> </li>
                </ul>
               <% }) %>
        <% } %>   
    </div>